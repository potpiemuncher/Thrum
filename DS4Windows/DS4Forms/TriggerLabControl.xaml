<UserControl x:Class="DS4WinWPF.DS4Forms.TriggerLabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="960"
             HorizontalAlignment="Left" VerticalAlignment="Stretch"
             Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Window}}, Converter={StaticResource ShellViewportWidth}, ConverterParameter=318}">
    <UserControl.Resources>
        <Style x:Key="LabCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="18" />
        </Style>
        <Style x:Key="LabToggle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="42" Height="23">
                            <Border x:Name="Track" Background="{DynamicResource BorderColor}" CornerRadius="12" />
                            <Ellipse x:Name="Knob" Width="17" Height="17" Margin="3" HorizontalAlignment="Left" Fill="{DynamicResource ForegroundColor}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentColor}"/><Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.7"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LabModeButton" TargetType="Button" BasedOn="{StaticResource BridgeSecondaryButtonStyle}">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
        <Style x:Key="LabSlider" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/><Setter Property="Maximum" Value="100"/><Setter Property="TickFrequency" Value="5"/><Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="Height" Value="28"/><Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer x:Name="triggerLabScroll" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  HorizontalContentAlignment="Stretch">
        <Grid Margin="0,0,0,12" HorizontalAlignment="Stretch"
              Width="{Binding ActualWidth, ElementName=triggerLabScroll}">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid x:Name="profileHeaderRow" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Border x:Name="overrideBadge" Margin="0,0,14,0" Padding="9,4" CornerRadius="4" Background="#4DFFB020" Visibility="Collapsed">
                        <TextBlock Text="LAB OVERRIDE" Foreground="#FFFFB020" FontSize="10" FontWeight="Bold" />
                    </Border>
                    <TextBlock Text="Effect design" Foreground="{DynamicResource MutedForegroundColor}"
                               FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button x:Name="linkedButton" Content="Linked" MinWidth="76" Height="36" Margin="0,0,6,0"
                            Click="LinkedButton_Click" ToolTipService.ShowDuration="15000"
                            ToolTip="Use one shared effect design for L2 and R2. Presets, modes, and sliders mirror between both sides; each trigger's Active switch remains independent." />
                    <Button x:Name="splitButton" Content="Split" MinWidth="76" Height="36" Margin="0,0,14,0"
                            Click="SplitButton_Click" ToolTipService.ShowDuration="15000"
                            ToolTip="Configure separate effect designs for L2 and R2. Returning to Split restores your last independent settings, including which trigger was active." />
                    <TextBlock Text="Enabled" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,9,0" />
                    <CheckBox x:Name="labEnabledToggle" Style="{StaticResource LabToggle}" Click="LabEnabledToggle_Click" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource LabCard}" Margin="0,0,0,16">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="User preset library" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="Saved under the selected Thrum data folder, independently of controller profiles."
                                       Foreground="{DynamicResource MutedForegroundColor}" TextWrapping="Wrap" />
                        </StackPanel>
                        <Border Grid.Column="1" Padding="8,3" CornerRadius="4"
                                Background="{DynamicResource RaisedBackgroundColor}"
                                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                            <TextBlock Text="USER PRESETS" Foreground="{DynamicResource AccentColor}"
                                       FontSize="10" FontWeight="Bold" />
                        </Border>
                    </Grid>
                    <Grid Margin="0,14,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <ComboBox x:Name="userPresetCombo" MinHeight="36" MinWidth="220"
                                  DisplayMemberPath="Name" SelectionChanged="UserPresetCombo_SelectionChanged" />
                        <WrapPanel Grid.Column="1" Margin="10,0,0,0">
                            <Button x:Name="applyUserPresetLeftButton" Content="Apply to L2" Margin="0,0,6,0"
                                    Style="{StaticResource BridgeSecondaryButtonStyle}" Click="ApplyUserPresetLeft_Click" />
                            <Button x:Name="applyUserPresetRightButton" Content="Apply to R2" Margin="0,0,6,0"
                                    Style="{StaticResource BridgeSecondaryButtonStyle}" Click="ApplyUserPresetRight_Click" />
                            <Button x:Name="renameUserPresetButton" Content="Rename" Margin="0,0,6,0"
                                    Style="{StaticResource BridgeSecondaryButtonStyle}" Click="RenameUserPreset_Click" />
                            <Button x:Name="deleteUserPresetButton" Content="Delete"
                                    Style="{StaticResource BridgeSecondaryButtonStyle}" Click="DeleteUserPreset_Click" />
                        </WrapPanel>
                    </Grid>
                    <WrapPanel Margin="0,10,0,0">
                        <!--
                            These two read the current profile's trigger effect,
                            so unlike the rest of the library row they do need a
                            controller; RefreshSettings gates them.
                        -->
                        <Button x:Name="saveLeftUserPresetButton" Content="Save L2 as preset" Margin="0,0,6,0"
                                Style="{StaticResource BridgeSecondaryButtonStyle}" Click="SaveLeftUserPreset_Click" />
                        <Button x:Name="saveRightUserPresetButton" Content="Save R2 as preset" Margin="0,0,14,0"
                                Style="{StaticResource BridgeSecondaryButtonStyle}" Click="SaveRightUserPreset_Click" />
                        <Button Content="Import JSON" Margin="0,0,6,0"
                                Style="{StaticResource BridgeSecondaryButtonStyle}" Click="ImportUserPresets_Click" />
                        <Button x:Name="exportUserPresetButton" Content="Export selected" Margin="0,0,6,0"
                                Style="{StaticResource BridgeSecondaryButtonStyle}" Click="ExportUserPreset_Click" />
                        <Button x:Name="exportAllUserPresetsButton" Content="Export all"
                                Style="{StaticResource BridgeSecondaryButtonStyle}" Click="ExportAllUserPresets_Click" />
                    </WrapPanel>
                    <TextBlock x:Name="presetLibraryStatusText" Margin="0,9,0,0"
                               Foreground="{DynamicResource MutedForegroundColor}" TextWrapping="Wrap"
                               Text="The user preset library has not been loaded yet." />
                </StackPanel>
            </Border>

            <Grid x:Name="profileSidesRow" Grid.Row="2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="260"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*" MinWidth="260"/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource LabCard}"><ContentControl x:Name="leftCard" /></Border>
                <Border Grid.Column="2" Style="{StaticResource LabCard}"><ContentControl x:Name="rightCard" /></Border>
            </Grid>

            <Border Grid.Row="3" Style="{StaticResource LabCard}" Padding="14" Margin="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" />
                    <TextBlock x:Name="labStatusText" Grid.Column="1" Margin="10,0" TextWrapping="Wrap" VerticalAlignment="Center" />
                    <TextBlock x:Name="labBehaviorText" Grid.Column="2" Text="Active lab effects override incoming game adaptive-trigger output." Foreground="{DynamicResource MutedForegroundColor}" TextWrapping="Wrap" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
