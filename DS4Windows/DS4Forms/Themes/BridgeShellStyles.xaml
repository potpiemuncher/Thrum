<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:identity="clr-namespace:DS4Windows">

    <!--
        DS5 Bridge-inspired application shell.

        This dictionary only owns layout and component geometry. Theme colors live in
        DefaultTheme.xaml and DarkTheme.xaml so the existing runtime theme switch keeps
        working without rebuilding any views.
    -->

    <Style x:Key="BridgeCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="Margin" Value="0,0,0,14" />
    </Style>

    <Style x:Key="BridgeSectionTitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

    <Style x:Key="BridgeSectionDescriptionStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <Style x:Key="BridgePrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource AccentColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="MinHeight" Value="36" />
    </Style>

    <Style x:Key="BridgeSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="MinHeight" Value="34" />
    </Style>

    <Style x:Key="BridgeProfileComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxStyle}">
        <Setter Property="Width" Value="196" />
        <Setter Property="Height" Value="42" />
        <Setter Property="MaxDropDownHeight" Value="280" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboBoxItemStyle}">
                    <Setter Property="Padding" Value="10,8" />
                    <Setter Property="Margin" Value="0,1" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="ItemChrome" Background="Transparent" CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource NavigationSelectionColor}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource NavigationSelectionColor}" />
                                        <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.75" />
                                        <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundColor}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="ProfileChrome" Background="{DynamicResource RaisedBackgroundColor}"
                                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                                CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="38" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Margin="12,0,7,0" VerticalAlignment="Center"
                                                  HorizontalAlignment="Stretch" IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding ItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                <Border Grid.Column="1" Background="{DynamicResource CardBackgroundColor}"
                                        BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,0,0,0"
                                        CornerRadius="0,6,6,0">
                                    <Path x:Name="ProfileArrow" Data="M 1 2 L 5 6 L 9 2"
                                          Width="10" Height="7" Stretch="Fill"
                                          Stroke="{DynamicResource MutedForegroundColor}" StrokeThickness="1.6"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </Border>
                            </Grid>
                        </Border>
                        <ToggleButton Background="Transparent" BorderThickness="0" Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent" />
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PopupAnimation="Fade">
                            <Border Margin="0,6,0,0" MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{DynamicResource CardBackgroundColor}"
                                    BorderBrush="{DynamicResource AccentColor}" BorderThickness="1"
                                    CornerRadius="6" Padding="4">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                              CanContentScroll="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ProfileChrome" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="ProfileArrow" Property="Stroke" Value="{DynamicResource AccentColor}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="ProfileChrome" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="ProfileChrome" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="ProfileChrome" Property="BorderThickness" Value="2" />
                            <Setter TargetName="ProfileArrow" Property="Stroke" Value="{DynamicResource AccentColor}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.75" />
                            <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeDescribedCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource CheckBoxStyle}">
        <Setter Property="Margin" Value="4,2,4,13" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Margin="3,0,0,0">
                        <TextBlock Text="{Binding}" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundColor}" />
                        <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=CheckBox}, Path=ToolTip}"
                                   Margin="0,3,0,0" MaxWidth="620" TextWrapping="Wrap" FontSize="11"
                                   Foreground="{DynamicResource MutedForegroundColor}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeControllerCardItemStyle" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="Card"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="18">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource SuccessColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeTriggerPairIconStyle" TargetType="ContentControl">
        <Setter Property="Width" Value="26" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Viewbox Width="26" Height="20" Stretch="Uniform">
                        <Canvas Width="26" Height="20">
                            <Path Data="M1.4,6.2 C1.8,2.5 4.2,1 9.8,1 L12,3.8 10.8,15.1 C10.5,17.6 8.4,19 5.8,18.2 L3.1,17.3 C1.7,16.8 1,15.3 1,13.7 Z"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="1.25"
                                  StrokeLineJoin="Round" Fill="{DynamicResource RaisedBackgroundColor}" />
                            <Path Data="M24.6,6.2 C24.2,2.5 21.8,1 16.2,1 L14,3.8 15.2,15.1 C15.5,17.6 17.6,19 20.2,18.2 L22.9,17.3 C24.3,16.8 25,15.3 25,13.7 Z"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="1.25"
                                  StrokeLineJoin="Round" Fill="{DynamicResource RaisedBackgroundColor}" />
                            <Path Data="M2.2,5.5 C4.6,4.2 7.7,4.1 10.7,5.1 M15.3,5.1 C18.3,4.1 21.4,4.2 23.8,5.5"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="0.85"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.8" />
                            <TextBlock Canvas.Left="3.2" Canvas.Top="7.1" Text="L2"
                                       FontFamily="Segoe UI" FontSize="6.3" FontWeight="Bold"
                                       Foreground="{TemplateBinding Foreground}" />
                            <TextBlock Canvas.Left="16.2" Canvas.Top="7.1" Text="R2"
                                       FontFamily="Segoe UI" FontSize="6.3" FontWeight="Bold"
                                       Foreground="{TemplateBinding Foreground}" />
                        </Canvas>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeNavigationTabItemStyle" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="NavItem"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="3,0,0,0"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="NavGlyph"
                                       Text="{TemplateBinding Tag}"
                                       FontFamily="Segoe Fluent Icons"
                                       Foreground="{DynamicResource MutedForegroundColor}"
                                       FontSize="14"
                                       Width="28" TextAlignment="Center"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                            <ContentControl x:Name="NavTriggerGlyph"
                                            Style="{StaticResource BridgeTriggerPairIconStyle}"
                                            Visibility="Collapsed" />
                            <ContentPresenter Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="NavItem" Property="Background" Value="{DynamicResource RaisedBackgroundColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="NavItem" Property="Background" Value="{DynamicResource NavigationSelectionColor}" />
                            <Setter TargetName="NavItem" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="NavGlyph" Property="Foreground" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="NavTriggerGlyph" Property="Foreground" Value="{DynamicResource AccentColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                        <Trigger Property="Tag" Value="TriggerPair">
                            <Setter TargetName="NavGlyph" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="NavTriggerGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.75" />
                            <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeEditorNavigationListItemStyle" TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Margin" Value="0,1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="EditorNavItem" Background="{TemplateBinding Background}"
                            BorderBrush="Transparent" BorderThickness="3,0,0,0"
                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!--
                                The rail's glyphs are general Unicode symbols
                                (arrows, geometric shapes), not private-use icon
                                codepoints. Segoe Fluent Icons contains only the
                                latter, so naming it alone rendered most of them
                                as missing-glyph boxes. The fallback list keeps
                                any glyph the icon font does have and resolves
                                the rest from the general-purpose faces.
                            -->
                            <TextBlock x:Name="EditorNavGlyph" Text="{TemplateBinding Tag}"
                                       FontFamily="Segoe Fluent Icons, Segoe UI Symbol, Segoe UI"
                                       Foreground="{DynamicResource MutedForegroundColor}"
                                       FontSize="13" Width="28" TextAlignment="Center"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <ContentControl x:Name="EditorNavTriggerGlyph"
                                            Style="{StaticResource BridgeTriggerPairIconStyle}"
                                            Visibility="Collapsed" />
                            <StackPanel Grid.Column="1">
                                <ContentPresenter VerticalAlignment="Center" />
                                <TextBlock Text="{TemplateBinding ToolTip}" Margin="0,1,0,0"
                                           FontSize="9" TextWrapping="Wrap"
                                           Foreground="{DynamicResource MutedForegroundColor}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="EditorNavItem" Property="Background" Value="{DynamicResource RaisedBackgroundColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="EditorNavItem" Property="Background" Value="{DynamicResource NavigationSelectionColor}" />
                            <Setter TargetName="EditorNavItem" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="EditorNavGlyph" Property="Foreground" Value="{DynamicResource AccentColor}" />
                            <Setter TargetName="EditorNavTriggerGlyph" Property="Foreground" Value="{DynamicResource AccentColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                        <Trigger Property="Tag" Value="TriggerPair">
                            <Setter TargetName="EditorNavGlyph" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="EditorNavTriggerGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeSidebarControllerItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="7" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="ControllerSelectorCard"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent" BorderThickness="1"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ControllerSelectorCard" Property="Background"
                                    Value="{DynamicResource RaisedBackgroundColor}" />
                            <Setter TargetName="ControllerSelectorCard" Property="BorderBrush"
                                    Value="{DynamicResource BorderColor}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ControllerSelectorCard" Property="Background"
                                    Value="{DynamicResource ControllerSelectionBackgroundColor}" />
                            <Setter TargetName="ControllerSelectorCard" Property="BorderBrush"
                                    Value="{DynamicResource AccentColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeMainTabControlStyle" TargetType="TabControl">
        <Setter Property="TabStripPlacement" Value="Left" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource BridgeNavigationTabItemStyle}" />
        <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid Background="{DynamicResource SurfaceBackgroundColor}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="238" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid>
                            <Border Background="{DynamicResource SidebarBackgroundColor}"
                                    BorderBrush="{DynamicResource BorderColor}"
                                    BorderThickness="0,0,1,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Opacity" Value="1" />
                                        <Setter Property="IsHitTestVisible" Value="True" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ProfileEditorMode}" Value="True">
                                                <Setter Property="Opacity" Value="0" />
                                                <Setter Property="IsHitTestVisible" Value="False" />
                                                <!--
                                                    Opacity alone hides a sidebar visually
                                                    while leaving every control in it
                                                    keyboard-focusable: Tab landed on the
                                                    hidden profile-editor rail from an
                                                    ordinary page. Focusable=False takes
                                                    the whole subtree out of the tab order
                                                    with it.
                                                -->
                                                <Setter Property="Focusable" Value="False" />
                                                <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.16" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid Margin="18,18,18,14">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="4,0,0,20">
                                        <Border Width="30" Height="30" CornerRadius="5"
                                                Background="{DynamicResource AccentColor}" Margin="0,0,10,0">
                                            <TextBlock Text="T" Foreground="White" FontSize="17" FontWeight="Bold"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{x:Static identity:ProductInfo.ProductName}"
                                                       FontSize="17" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource ForegroundColor}" />
                                            <TextBlock Text="Controller companion" FontSize="10"
                                                       Foreground="{DynamicResource MutedForegroundColor}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Grid.Row="1" Background="{DynamicResource CardBackgroundColor}"
                                            BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                                            CornerRadius="4" Padding="5" Margin="0,0,0,18">
                                        <Grid>
                                            <ListBox x:Name="SidebarControllerList" ItemsSource="{Binding ControllerCol}"
                                                     SelectedItem="{Binding SelectedController, Mode=TwoWay}"
                                                     ItemContainerStyle="{StaticResource BridgeSidebarControllerItemStyle}"
                                                     Background="Transparent" BorderThickness="0" MaxHeight="124"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="48" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <Image Source="{Binding ControllerImageSource}" Width="42" Height="32"
                                                                   Stretch="Uniform" VerticalAlignment="Center"
                                                                   RenderOptions.BitmapScalingMode="HighQuality" />
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="3,0,6,0">
                                                                <TextBlock Text="{Binding ControllerDisplayName}" TextTrimming="CharacterEllipsis"
                                                                           FontSize="11" FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource ForegroundColor}" />
                                                                <TextBlock Text="{Binding SelectedProfile, StringFormat='Profile: {0}'}"
                                                                           TextTrimming="CharacterEllipsis" FontSize="9"
                                                                           Foreground="{DynamicResource MutedForegroundColor}"
                                                                           ToolTip="{Binding SelectedProfile}" />
                                                            </StackPanel>
                                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                                <TextBlock Text="{Binding BatteryState}" FontSize="10" FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource SuccessColor}" HorizontalAlignment="Right" />
                                                                <Image Source="{Binding StatusSource}" Width="12" Height="12"
                                                                       Margin="0,3,0,0" HorizontalAlignment="Right" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                            <TextBlock Text="Connect a controller to begin" TextWrapping="Wrap"
                                                       Foreground="{DynamicResource MutedForegroundColor}" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=SidebarControllerList, Path=HasItems}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </Border>

                                    <TextBlock Grid.Row="2" Text="NAVIGATION" Margin="10,0,0,8"
                                               FontSize="10" FontWeight="Bold"
                                               Foreground="{DynamicResource MutedForegroundColor}" />
                                    <ScrollViewer Grid.Row="3"
                                                  VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  Focusable="False">
                                        <TabPanel IsItemsHost="True"
                                                  KeyboardNavigation.TabNavigation="Once" />
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <Border Panel.ZIndex="1" Background="{DynamicResource SidebarBackgroundColor}"
                                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Opacity" Value="0" />
                                        <Setter Property="IsHitTestVisible" Value="False" />
                                        <!--
                                            Opacity alone hides a sidebar visually
                                            while leaving every control in it
                                            keyboard-focusable: Tab landed on the
                                            hidden profile-editor rail from an
                                            ordinary page. Focusable=False takes
                                            the whole subtree out of the tab order
                                            with it.
                                        -->
                                        <Setter Property="Focusable" Value="False" />
                                        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ProfileEditorMode}" Value="True">
                                                <Setter Property="Opacity" Value="1" />
                                                <Setter Property="IsHitTestVisible" Value="True" />
                                                <Setter Property="Focusable" Value="True" />
                                                <Setter Property="KeyboardNavigation.TabNavigation" Value="Continue" />
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.20" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.14" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid Margin="18,18,18,14">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="4,0,0,16">
                                        <Border Width="30" Height="30" CornerRadius="5"
                                                Background="{DynamicResource AccentColor}" Margin="0,0,10,0">
                                            <TextBlock Text="P" Foreground="White" FontSize="17" FontWeight="Bold"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Profile Editor" FontSize="17" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource ForegroundColor}" />
                                            <TextBlock Text="Focused workspace" FontSize="10"
                                                       Foreground="{DynamicResource MutedForegroundColor}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Grid.Row="1" Background="{DynamicResource CardBackgroundColor}"
                                            BorderBrush="{DynamicResource AccentColor}" BorderThickness="1"
                                            CornerRadius="4" Padding="11" Margin="0,0,0,14">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding EditingControllerImageSource}"
                                                   Width="42" Height="34" Margin="0,0,6,0"
                                                   Stretch="Uniform" VerticalAlignment="Center"
                                                   RenderOptions.BitmapScalingMode="HighQuality" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="EDITING PROFILE" FontSize="9" FontWeight="Bold"
                                                           Foreground="{DynamicResource AccentColor}" />
                                                <TextBlock Text="{Binding EditingProfileName}" Margin="0,3,0,0"
                                                           FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding EditingProfileName}"
                                                           Foreground="{DynamicResource ForegroundColor}" />
                                                <TextBlock Text="{Binding EditingControllerName}"
                                                           Margin="0,2,0,0" FontSize="9" TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding EditingControllerName}"
                                                           Foreground="{DynamicResource MutedForegroundColor}" />
                                                <TextBlock Text="{Binding EditingControllerConnection}"
                                                           Margin="0,1,0,0" FontSize="9" TextTrimming="CharacterEllipsis"
                                                           Foreground="{DynamicResource MutedForegroundColor}" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <TextBlock Grid.Row="2" Text="PROFILE WORKSPACE" Margin="10,0,0,7"
                                               FontSize="10" FontWeight="Bold"
                                               Foreground="{DynamicResource MutedForegroundColor}" />

                                    <ListBox Grid.Row="3" Background="Transparent" BorderThickness="0"
                                             SelectedIndex="{Binding ProfileEditorNavigationIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             ItemContainerStyle="{StaticResource BridgeEditorNavigationListItemStyle}"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBoxItem Content="Back to Profiles" Tag="&#x2190;" ToolTip="Cancel editing and return to the profile list" />
                                        <ListBoxItem Content="Button Mapping" Tag="&#x25A3;" ToolTip="Buttons, sticks, touch gestures, and shortcuts" />
                                        <ListBoxItem Content="Special Actions" Tag="&#x2726;" ToolTip="Macros, launches, shifts, and multi-actions" />
                                        <ListBoxItem Content="Controller Readings" Tag="&#x223F;" ToolTip="Live inputs and calibration" />
                                        <ListBoxItem Content="Axis Config" Tag="&#x2194;" ToolTip="Sticks, triggers, dead zones, and curves" />
                                        <ListBoxItem Content="Lightbar" Tag="&#x25C9;" ToolTip="Color, battery, flash, and charging" />
                                        <ListBoxItem Content="Touchpad" Tag="&#x25A1;" ToolTip="Mouse, gestures, passthrough, and positioning" />
                                        <ListBoxItem Content="Gyro" Tag="&#x25CE;" ToolTip="Motion aiming, steering, and swipes" />
                                        <ListBoxItem Content="Audio Haptics" Tag="&#xE767;" ToolTip="Turn system or app audio into advanced haptics" />
                                        <ListBoxItem Content="Trigger Lab" Tag="TriggerPair" ToolTip="Build persistent adaptive-trigger effects" />
                                        <ListBoxItem Content="Advanced" Tag="&#x2699;" ToolTip="Output, rumble, latency, and compatibility" />
                                        <ListBoxItem Content="Log" Tag="&#x2261;" ToolTip="Service events without leaving the editor" />
                                    </ListBox>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" MinHeight="84" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1"
                                    Padding="28,16" Background="{DynamicResource SurfaceBackgroundColor}">
                                <Grid>
                                    <StackPanel VerticalAlignment="Center">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProfileEditorMode}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <ContentControl Content="{Binding SelectedItem.Header, RelativeSource={RelativeSource TemplatedParent}}"
                                                        Foreground="{DynamicResource ForegroundColor}"
                                                        FontSize="24" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding SelectedItem.ToolTip, RelativeSource={RelativeSource TemplatedParent}}"
                                                   Margin="0,3,0,0" FontSize="12" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource MutedForegroundColor}" />
                                    </StackPanel>
                                    <StackPanel VerticalAlignment="Center">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProfileEditorMode}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="{Binding ProfileEditorSectionTitle}"
                                                   Foreground="{DynamicResource ForegroundColor}"
                                                   FontSize="24" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding ProfileEditorSectionDescription}"
                                                   Margin="0,3,0,0" FontSize="12" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource MutedForegroundColor}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ContentPresenter Grid.Row="1" x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent" Margin="28,22"
                                              HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeProfileTabItemStyle" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="14,9" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="ProfileNavItem" Background="{TemplateBinding Background}"
                            BorderBrush="Transparent" BorderThickness="3,0,0,0"
                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <StackPanel>
                            <ContentPresenter ContentSource="Header" />
                            <TextBlock Text="{TemplateBinding Tag}" Margin="0,2,0,0" FontSize="10"
                                       TextWrapping="Wrap" Foreground="{DynamicResource MutedForegroundColor}" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ProfileNavItem" Property="Background" Value="{DynamicResource RaisedBackgroundColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ProfileNavItem" Property="Background" Value="{DynamicResource NavigationSelectionColor}" />
                            <Setter TargetName="ProfileNavItem" Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BridgeProfileTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter ContentSource="SelectedContent" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Status badge.

        Colour carries meaning here, so it is stated once: green only for a
        production-approved package, amber for a package we recognise but that
        is still experimental, red for one we could not verify, and neutral grey
        for "absent" and for "not established yet". Amber is deliberately not
        green: a manifest match is identity evidence, never approval.

        Triggers bind to the view model's BadgeKind token rather than to the
        readiness enum, because "Checking" and "Unknown" are not readiness
        states and a nullable enum does not trigger reliably.
    -->
    <Style x:Key="BridgeStatusBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="10,3" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Unverified">
                <Setter Property="BorderBrush" Value="{DynamicResource DangerColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Experimental">
                <Setter Property="BorderBrush" Value="{DynamicResource WarningColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Approved">
                <Setter Property="BorderBrush" Value="{DynamicResource SuccessColor}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="BridgeStatusBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Unverified">
                <Setter Property="Foreground" Value="{DynamicResource DangerColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Experimental">
                <Setter Property="Foreground" Value="{DynamicResource WarningColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding BadgeKind}" Value="Approved">
                <Setter Property="Foreground" Value="{DynamicResource SuccessColor}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Neutral subsystem tag used by dense log rows. Unlike status badges,
         it has no readiness trigger and therefore needs no BadgeKind binding. -->
    <Style x:Key="BridgeLogCategoryBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="8,2" />
        <Setter Property="Margin" Value="0,1" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>

    <!-- One "why" line in a reason or identity list. -->
    <Style x:Key="BridgeCardListItemStyle" TargetType="TextBlock"
           BasedOn="{StaticResource BridgeSectionDescriptionStyle}">
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

    <!--
        Inline notice above a page's content. Same colour vocabulary as the
        status badge, one step louder: red when the page cannot do its job,
        amber when it can but a capability is deliberately switched off.
        Triggers on the view model's Severity token for the same reason the
        badge triggers on BadgeKind.
    -->
    <Style x:Key="BridgeInlineBannerStyle" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="10,8" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Severity}" Value="Blocked">
                <Setter Property="BorderBrush" Value="{DynamicResource DangerColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Severity}" Value="Limited">
                <Setter Property="BorderBrush" Value="{DynamicResource WarningColor}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="BridgeInlineBannerHeadlineStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MutedForegroundColor}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,4" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Severity}" Value="Blocked">
                <Setter Property="Foreground" Value="{DynamicResource DangerColor}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Severity}" Value="Limited">
                <Setter Property="Foreground" Value="{DynamicResource WarningColor}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
